{% extends 'base.html.twig' %}

{% block title %}SiteClientToServicesBinder index{% endblock %}

{% block body %}
    <h1>SiteClientToServicesBinder index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for site_client_to_services_binder in site_client_to_services_binders %}
            <tr>
                <td>{{ site_client_to_services_binder.id }}</td>
                <td>
                    <a href="{{ path('site_client_to_services_binder_show', {'id': site_client_to_services_binder.id}) }}">show</a>
                    <a href="{{ path('site_client_to_services_binder_edit', {'id': site_client_to_services_binder.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('site_client_to_services_binder_new') }}">Create new</a>
{% endblock %}
