{% extends 'base.html.twig' %}

{% block title %}FrenchEchoppe{% endblock %}

{% block body %}
    <h1>FrenchEchoppe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ french_echoppe.id }}</td>
            </tr>
            <tr>
                <th>OnlineDate</th>
                <td>{{ french_echoppe.onlineDate ? french_echoppe.onlineDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Online</th>
                <td>{{ french_echoppe.online ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Provider</th>
                <td>{{ french_echoppe.provider }}</td>
            </tr>
            <tr>
                <th>Offer</th>
                <td>{{ french_echoppe.offer }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ french_echoppe.cost }}</td>
            </tr>
            <tr>
                <th>InvoicedPrice</th>
                <td>{{ french_echoppe.invoicedPrice }}</td>
            </tr>
            <tr>
                <th>RenewalType</th>
                <td>{{ french_echoppe.renewalType }}</td>
            </tr>
            <tr>
                <th>RenewalDate</th>
                <td>{{ french_echoppe.renewalDate ? french_echoppe.renewalDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ french_echoppe.startDate ? french_echoppe.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Commentary</th>
                <td>{{ french_echoppe.commentary }}</td>
            </tr>
            <tr>
                <th>Enable</th>
                <td>{{ french_echoppe.enable ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>SiteClientToServicesBinderId</th>
                <td>{{ french_echoppe.siteClientToServicesBinderId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('french_echoppe_index') }}">back to list</a>

    <a href="{{ path('french_echoppe_edit', {'id': french_echoppe.id}) }}">edit</a>

    {{ include('french_echoppe/_delete_form.html.twig') }}
{% endblock %}
