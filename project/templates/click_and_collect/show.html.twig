{% extends 'base.html.twig' %}

{% block title %}ClickAndCollect{% endblock %}

{% block body %}
    <h1>ClickAndCollect</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ click_and_collect.id }}</td>
            </tr>
            <tr>
                <th>OnlineDate</th>
                <td>{{ click_and_collect.onlineDate ? click_and_collect.onlineDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Online</th>
                <td>{{ click_and_collect.online ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Enable</th>
                <td>{{ click_and_collect.enable ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Provider</th>
                <td>{{ click_and_collect.provider }}</td>
            </tr>
            <tr>
                <th>Offer</th>
                <td>{{ click_and_collect.offer }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ click_and_collect.cost }}</td>
            </tr>
            <tr>
                <th>InvoicedPrice</th>
                <td>{{ click_and_collect.invoicedPrice }}</td>
            </tr>
            <tr>
                <th>RenewalType</th>
                <td>{{ click_and_collect.renewalType }}</td>
            </tr>
            <tr>
                <th>RenewalDate</th>
                <td>{{ click_and_collect.renewalDate ? click_and_collect.renewalDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>StartDate</th>
                <td>{{ click_and_collect.startDate ? click_and_collect.startDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>SiteClientToServicesBinderId</th>
                <td>{{ click_and_collect.siteClientToServicesBinderId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('click_and_collect_index') }}">back to list</a>

    <a href="{{ path('click_and_collect_edit', {'id': click_and_collect.id}) }}">edit</a>

    {{ include('click_and_collect/_delete_form.html.twig') }}
{% endblock %}
