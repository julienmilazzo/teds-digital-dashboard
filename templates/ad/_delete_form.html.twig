<form method="post" action="{{ path('ad_delete', {'id': currentAd.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir le supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ currentAd.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
